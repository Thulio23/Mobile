import { StyleSheet } from "react-native"

export const colors = {
    black: "#000",
    white: "#FFF",
    primary: "#FF0000",
    
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})